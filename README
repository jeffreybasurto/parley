Parley is a hosted API for quickly, easily and securely adding scalable realtime functionality via WebSockets to web and mobile apps.



Parley uses node.  To start the app once node is installed from commandline: 

node app.js

This will start the service. 